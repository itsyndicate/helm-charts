apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "django.fullname" . }}
  labels:
    {{- include "django.labels" . | nindent 4 }}
  annotations:
    secret.reloader.stakater.com/reload: "{{ include "django.fullname" . }}-env-secrets{{ with .Values.django.env.existingSecretName }}, {{ . }}{{ end }}, {{ include "django.fullname" . }}-env-ext-secrets"
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  {{- if not (kindIs "invalid" .Values.revisionHistoryLimit) }}
  revisionHistoryLimit: {{ .Values.revisionHistoryLimit }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "django.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "django.labels" . | nindent 8 }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "django.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        {{- if .Values.nginx.enabled }}
        - name: {{ .Chart.Name }}-nginx
          securityContext:
            {{- toYaml .Values.nginx.securityContext | nindent 12 }}
          image: "{{ .Values.nginx.image.repository }}:{{ .Values.nginx.image.tag | default "alpine" }}"
          imagePullPolicy: {{ .Values.nginx.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
              protocol: TCP
          livenessProbe:
            {{- toYaml .Values.nginx.livenessProbe | nindent 12 }}
          readinessProbe:
            {{- toYaml .Values.nginx.readinessProbe | nindent 12 }}
          resources:
            {{- toYaml .Values.nginx.resources | nindent 12 }}
          volumeMounts:
            - name: socket
              mountPath: /tmp
            - name: nginx-conf
              mountPath: /etc/nginx/nginx.conf
              subPath: nginx.conf
              readOnly: true
          {{- with .Values.nginx.volumeMounts }}
            {{- toYaml . | nindent 12 }}
          {{- end }}
        {{- end }}
        - name: {{ .Chart.Name }}-django-app
          securityContext:
            {{- toYaml .Values.django.securityContext | nindent 12 }}
          image: "{{ .Values.django.image.repository }}:{{ .Values.django.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.django.image.pullPolicy }}
          {{- if .Values.django.command }}
          command: {{ toJson .Values.django.command }}
          {{- else }}
          command: [ "uvicorn" ]
          {{- end }}
          {{- if .Values.django.args }}
          args: {{ toJson .Values.django.args }}
          {{- else if not .Values.nginx.enabled }}
          args: [ "--host", "0.0.0.0", "--port", "{{ .Values.django.port }}", "--workers", "3", "main:app" ]
          {{- else }}
          args: [ "--uds", "/tmp/uvicorn.sock", "--workers", "3", "main:app" ]
          {{- end }}
          envFrom:
            - secretRef:
                name: {{ include "django.fullname" . }}-env-secrets
            {{- if .Values.django.env.existingSecretName }}
            - secretRef:
                name: {{ .Values.django.env.existingSecretName }}
            {{- end }}
            {{- if .Values.django.env.envFromSecretsManager.enabled }}
            - secretRef:
                name: {{ include "django.fullname" . }}-env-ext-secrets
            {{- end }}
          {{- if not .Values.nginx.enabled }}
          ports:
            - name: http
              containerPort: {{ .Values.django.port }}
              protocol: TCP
          {{- end }}
          {{- with .Values.django.livenessProbe }}
          livenessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          readinessProbe:
            {{- if .Values.django.readinessProbe }}
            {{- toYaml .Values.django.readinessProbe | nindent 12 }}
            {{- else if .Values.nginx.enabled }}
            exec:
              command:
                - ls
                - /tmp/uvicorn.sock
            initialDelaySeconds: 5
            periodSeconds: 5
            {{- else }}
            tcpSocket:
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
            {{- end }}
          resources:
            {{- toYaml .Values.django.resources | nindent 12 }}
          volumeMounts:
            {{- if .Values.nginx.enabled }}
            - name: socket
              mountPath: /tmp
            {{- end }}
          {{- with .Values.django.volumeMounts }}
            {{- toYaml . | nindent 12 }}
          {{- end }}
      volumes:
        {{- if .Values.nginx.enabled }}
        - name: socket
          emptyDir: { }
        {{- end }}
        {{- if .Values.nginx.enabled }}
        - name: nginx-conf
          configMap:
            name: {{ include "django.fullname" $ }}-nginx-conf
            items:
              - key: nginx.conf
                path: nginx.conf
        {{- with .Values.nginx.volumes }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- end }}
      {{- with .Values.django.volumes }}
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
