{{- if not .Values.nodejs.env.existingSecretName }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "nodejs.fullname" . }}-env-secrets
type: Opaque
data:
  {{- range $key, $val := .Values.nodejs.env.variables }}
  {{ $key }}: {{ $val | b64enc }}
  {{- end}}
{{- end }}

{{- if .Values.nodejs.env.envFromSecretsManager.enabled }}
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ include "nodejs.fullname" . }}-env-ext-secrets
spec:
  refreshInterval: 1m
  secretStoreRef:
    name: global-secret-store
    kind: ClusterSecretStore
  target:
    name: {{ include "nodejs.fullname" . }}-env-ext-secrets
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: {{ .Values.nodejs.env.envFromSecretsManager.secretPath }}
{{- end }}
