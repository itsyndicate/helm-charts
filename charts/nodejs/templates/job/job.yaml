{{- if .Values.nodejs.preDeployJob.enabled -}}
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "nodejs.fullname" . }}-pre-deploy
  labels:
    app.kubernetes.io/component: pre-deploy-job
  annotations:
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-weight": "-1"
    "helm.sh/hook-delete-policy": before-hook-creation
spec:
  backoffLimit: {{ .Values.nodejs.preDeployJob.backoffLimit }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "nodejs.labels" . | nindent 8 }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.nodejs.securityContext | nindent 12 }}
          image: "{{ .Values.nodejs.image.repository }}:{{ .Values.nodejs.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.nodejs.image.pullPolicy }}
          envFrom:
            - secretRef:
                name: {{ .Values.nodejs.env.existingSecretName | default (printf "%s-env-pre-deploy-secrets" (include "nodejs.fullname" .)) }}
            {{- if .Values.nodejs.env.envFromSecretsManager.enabled }}
            - secretRef:
                name: {{ include "nodejs.fullname" . }}-env-ext-secrets
            {{- end }}
          {{- with .Values.nodejs.preDeployJob.command }}
          command:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.nodejs.preDeployJob.args }}
          args:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          resources:
            {{- toYaml .Values.nodejs.resources | nindent 12 }}
          {{- with .Values.nodejs.volumeMounts }}
          volumeMounts:
            {{- toYaml . | nindent 12 }}
          {{- end }}
      {{- with .Values.nodejs.volumes }}
      volumes:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      restartPolicy: Never
{{- end -}}
