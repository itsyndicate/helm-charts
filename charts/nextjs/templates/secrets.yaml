# Plain (non-secret) extra env vars from .Values.nextjs.env.variables.
# Always rendered so these vars are injected even when an existingSecretName
# is set — the two are additive, not mutually exclusive.
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "nextjs.fullname" . }}-env-secrets
type: Opaque
data:
  {{- range $key, $val := .Values.nextjs.env.variables }}
  {{ $key }}: {{ $val | b64enc }}
  {{- end}}

{{- include "nextjs.validateSecretsManager" . }}
{{- $sm := .Values.nextjs.env.envFromSecretsManager }}
{{- if $sm.enabled }}
{{- $paths := $sm.secretPaths | default (list $sm.secretPath) }}
{{- $names := include "nextjs.extSecretNames" . | splitList "," }}
{{- range $i, $path := $paths }}
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ index $names $i }}
spec:
  refreshInterval: {{ $sm.refreshInterval | default "1m" }}
  secretStoreRef:
    name: {{ $sm.secretStoreName | default "global-secret-store" }}
    kind: {{ $sm.secretStoreKind | default "ClusterSecretStore" }}
  target:
    name: {{ index $names $i }}
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: {{ $path }}
{{- end }}
{{- end }}
